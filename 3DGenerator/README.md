# 3DGenerator

Starter prototype for the template-based 3D generation pipeline:

Prompt -> prompt analysis -> displacement maps -> template search -> deformation -> cleanup -> export

## Run

```bash
python main.py --prompt "wooden crate"
```
